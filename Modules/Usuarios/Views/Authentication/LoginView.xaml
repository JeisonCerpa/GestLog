<UserControl x:Class="GestLog.Modules.Usuarios.Views.Authentication.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="600">
    <UserControl.Resources>
        <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Canvas.Left)">
                <LinearDoubleKeyFrame KeyTime="0:0:0.0" Value="0"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.05" Value="-8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.1" Value="8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.15" Value="-8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.2" Value="8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.25" Value="-4"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.3" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>
    <Grid Background="{DynamicResource BackgroundAppBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0"
                    Width="540"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Padding="34"
                    CornerRadius="28"
                    Background="#1DFFFFFF"
                    BorderBrush="#55FFFFFF"
                    BorderThickness="1"
                    Effect="{StaticResource ThemeCardShadow}">
                <StackPanel HorizontalAlignment="Center">
                    <Image Source="/Assets/Simics.png"
                           Width="420"
                           Height="96"
                           Stretch="Uniform"
                           Margin="0,0,0,18"/>

                    <TextBlock Text="GestLog"
                               FontSize="34"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextOnPrimaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="Sistema de Gestión Logística"
                               FontSize="13"
                               Foreground="{DynamicResource TextOnPrimaryBrush}"
                               Opacity="0.82"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,22"/>

                    <StackPanel Width="340">
                        <Border x:Name="ErrorBorder"
                                Background="#22FFFFFF"
                                BorderBrush="#55F59E0B"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="10,8"
                                Margin="0,0,0,12"
                                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="0" Opacity="0.12"/>
                            </Border.Effect>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚠️" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource TextOnPrimaryBrush}"/>
                                <TextBlock Text="{Binding ErrorMessage}" FontSize="12" Foreground="{DynamicResource TextOnPrimaryBrush}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Usuario" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource TextOnPrimaryBrush}" Margin="0,0,0,6"/>
                            <Grid>
                                <TextBox x:Name="UsernameTextBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Tag="Ingrese su usuario" Style="{StaticResource ThemeTextBoxStyle}" TabIndex="1" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" TextChanged="UsernameTextBox_TextChanged">
                                    <TextBox.InputBindings>
                                        <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                                    </TextBox.InputBindings>
                                </TextBox>
                                <TextBlock x:Name="UsernameValidation" Text="✓" Foreground="{DynamicResource SuccessBrush}" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" Visibility="Collapsed"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Contraseña" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource TextOnPrimaryBrush}" Margin="0,0,0,6"/>
                            <Grid>
                                <PasswordBox x:Name="PasswordBox" Style="{StaticResource ThemePasswordBoxStyle}" Tag="Ingrese su contraseña" TabIndex="2" PasswordChanged="PasswordBox_PasswordChanged" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" Foreground="{DynamicResource TextOnPrimaryBrush}" CaretBrush="{DynamicResource TextOnPrimaryBrush}">
                                    <PasswordBox.InputBindings>
                                        <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                                    </PasswordBox.InputBindings>
                                </PasswordBox>
                                <TextBlock x:Name="CapsLockIndicator" Text="Caps Lock activado" Foreground="{DynamicResource DangerBrush}" FontSize="12" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,8,0" Visibility="Collapsed"/>
                            </Grid>
                        </StackPanel>

                        <Grid Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{Binding RememberMe}" Content="Recordar mi sesión" FontSize="12" Foreground="{DynamicResource TextOnPrimaryBrush}" Opacity="0.88" TabIndex="3" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="¿Olvidó contraseña?" Command="{Binding ForgotPasswordCommand}" Style="{StaticResource ThemeLinkButtonStyle}" TabIndex="4" VerticalAlignment="Center"/>
                        </Grid>

                        <Button Content="Iniciar Sesión" Style="{StaticResource ThemePrimaryButtonStyle}" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" Command="{Binding LoginCommand}" TabIndex="5" Margin="0,18,0,0"/>

                        <Grid Width="28" Height="28" Margin="0,14,0,8" HorizontalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Ellipse Width="24" Height="24" StrokeThickness="4" Stroke="{DynamicResource PrimaryBrush}" StrokeDashArray="2 4" StrokeDashCap="Round" RenderTransformOrigin="0.5,0.5" CacheMode="BitmapCache">
                                <Ellipse.RenderTransform>
                                    <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                                </Ellipse.RenderTransform>
                                <Ellipse.Triggers>
                                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:0.8"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Ellipse.Triggers>
                            </Ellipse>
                        </Grid>

                        <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="{DynamicResource TextOnPrimaryBrush}" Opacity="0.82" HorizontalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}" Margin="0,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,24,0,0">
            <TextBlock Text="© 2025 GestLog - Sistema de Gestión Logística" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="Versión 1.0.0" FontSize="9" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
