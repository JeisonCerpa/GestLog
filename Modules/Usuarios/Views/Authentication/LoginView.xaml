<UserControl x:Class="GestLog.Modules.Usuarios.Views.Authentication.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="600">
    <UserControl.Resources>
        <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Canvas.Left)">
                <LinearDoubleKeyFrame KeyTime="0:0:0.0" Value="0"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.05" Value="-8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.1" Value="8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.15" Value="-8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.2" Value="8"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.25" Value="-4"/>
                <LinearDoubleKeyFrame KeyTime="0:0:0.3" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="/Assets/Simics.png" Width="420" Height="96" Stretch="Uniform" Margin="{StaticResource ThemeMarginBottomLarge}"/>
                <TextBlock Text="GestLog" Style="{DynamicResource HeadingLargeStyle}" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center" Margin="{StaticResource ThemeMarginBottomLarge}"/>
                <TextBlock Text="Sistema de Gestión Logística" Style="{DynamicResource HeadingMediumStyle}" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="{StaticResource ThemeMarginBottomLarge}"/>

                <StackPanel HorizontalAlignment="Center" Width="340">
                    <Border x:Name="ErrorBorder" Background="#FFF3CD" BorderBrush="#FFC107" BorderThickness="1" CornerRadius="6" Padding="10,8" Margin="{StaticResource ThemeMarginBottomSmall}" Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Border.Effect>
                            <DropShadowEffect Color="#FFC107" BlurRadius="4" ShadowDepth="0" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding ErrorMessage}" FontSize="12" Foreground="#856404" TextWrapping="Wrap" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Margin="{StaticResource ThemeMarginBottomSmall}">
                        <TextBlock Text="Usuario" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" Margin="{StaticResource ThemeMarginBottomXSmall}"/>
                        <Grid>
                            <TextBox x:Name="UsernameTextBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Tag="Ingrese su usuario" Style="{StaticResource ThemeTextBoxStyle}" TabIndex="1" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" TextChanged="UsernameTextBox_TextChanged">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            <TextBlock x:Name="UsernameValidation" Text="✓" Foreground="Green" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" Visibility="Collapsed"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Margin="{StaticResource ThemeMarginBottomSmall}">
                        <TextBlock Text="Contraseña" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" Margin="{StaticResource ThemeMarginBottomXSmall}"/>
                        <Grid>
                            <PasswordBox x:Name="PasswordBox" Style="{StaticResource ThemePasswordBoxStyle}" Tag="Ingrese su contraseña" TabIndex="2" PasswordChanged="PasswordBox_PasswordChanged" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                                <PasswordBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                                </PasswordBox.InputBindings>
                            </PasswordBox>
                            <TextBlock x:Name="CapsLockIndicator" Text="Caps Lock activado" Foreground="Red" FontSize="12" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,8,0" Visibility="Collapsed"/>
                        </Grid>
                    </StackPanel>

                    <Grid Margin="{StaticResource ThemeMarginTopXSmall}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" IsChecked="{Binding RememberMe}" Content="Recordar mi sesión" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" TabIndex="3" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="¿Olvidó contraseña?" Command="{Binding ForgotPasswordCommand}" Style="{StaticResource ThemeLinkButtonStyle}" TabIndex="4" VerticalAlignment="Center"/>
                    </Grid>

                    <Button Content="Iniciar Sesión" Style="{StaticResource ThemePrimaryButtonStyle}" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" Command="{Binding LoginCommand}" TabIndex="5" Margin="{StaticResource ThemeMarginTopLarge}"/>

                    <Grid Width="28" Height="28" Margin="{StaticResource ThemeMarginTopMediumBottomSmall}" HorizontalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Ellipse Width="24" Height="24" StrokeThickness="4" Stroke="{DynamicResource PrimaryBrush}" StrokeDashArray="2 4" StrokeDashCap="Round" RenderTransformOrigin="0.5,0.5" CacheMode="BitmapCache">
                            <Ellipse.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:0.8"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </Grid>

                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}" Margin="0,0,0,12"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,24,0,0">
            <TextBlock Text="© 2025 GestLog - Sistema de Gestión Logística" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="Versión 1.0.0" FontSize="9" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
