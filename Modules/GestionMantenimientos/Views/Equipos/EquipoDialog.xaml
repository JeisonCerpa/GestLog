<Window x:Class="GestLog.Modules.GestionMantenimientos.Views.Equipos.EquipoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:GestLog.Converters"
        xmlns:behaviors="clr-namespace:GestLog.Behaviors"
        Title="Equipo"
        Width="1040"
        MinWidth="920"
        MinHeight="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="Manual"
        ShowInTaskbar="False"
        Loaded="EquipoDialog_Loaded">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:DecimalToCopCurrencyConverter x:Key="DecimalToCopCurrencyConverter" />
    </Window.Resources>

    <Grid x:Name="RootGrid"
          Background="{DynamicResource ModalOverlayBrush}"
          MouseLeftButtonDown="Overlay_MouseLeftButtonDown"
          UseLayoutRounding="True"
          SnapsToDevicePixels="True">

        <Border x:Name="Card"
                Width="1000"
                MaxHeight="960"
                Background="{DynamicResource BackgroundAppBrush}"
                CornerRadius="18"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Effect="{StaticResource ThemeCardShadow}"
                MouseLeftButtonDown="Panel_MouseLeftButtonDown"
                UseLayoutRounding="True"
                SnapsToDevicePixels="True"
                RenderOptions.BitmapScalingMode="HighQuality">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Style="{StaticResource ThemeHeaderContainerStyle}"
                        CornerRadius="18,18,0,0"
                        Padding="26,22">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="56"
                                    Height="56"
                                    CornerRadius="28"
                                    Background="{DynamicResource PrimaryLightBrush}"
                                    BorderBrush="{DynamicResource PrimaryBrush}"
                                    BorderThickness="1"
                                    Margin="0,0,16,0">
                                <TextBlock Text="⚙"
                                           FontSize="26"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Equipo"
                                           Style="{StaticResource HeaderTextStyle}"/>
                                <TextBlock Text="Alta, edición y control de la ficha del activo"
                                           Style="{StaticResource SubHeaderTextStyle}"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="1"
                                x:Name="BtnCerrar"
                                Style="{StaticResource CloseButton}"
                                Click="BtnCerrar_Click"
                                ToolTip="Cerrar (Esc)"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Width="38"
                                Height="38">
                            <TextBlock Text="✕"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextOnPrimaryBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Button>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Padding="26"
                              UseLayoutRounding="True"
                              SnapsToDevicePixels="True">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.05*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="0.95*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <Border Style="{StaticResource CardSectionStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Identidad del equipo" Style="{StaticResource SectionHeaderStyle}"/>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="16"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Código *" Style="{StaticResource LabelTextStyle}"/>
                                                <TextBox x:Name="CodigoTextBox"
                                                         Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}"
                                                         IsReadOnly="{Binding Path=IsCodigoReadOnly}"
                                                         IsEnabled="{Binding Path=IsCodigoEnabled}"
                                                         Style="{StaticResource ModalTextBoxStyle}"/>
                                                <TextBlock Text="{Binding MensajeCodigoDuplicado}"
                                                           Foreground="{DynamicResource DangerBrush}"
                                                           FontSize="11"
                                                           Margin="2,6,0,0"
                                                           Visibility="{Binding IsCodigoDuplicado, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="Nombre *" Style="{StaticResource LabelTextStyle}"/>
                                                <TextBox x:Name="NombreTextBox"
                                                         Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}"
                                                         Style="{StaticResource ModalTextBoxStyle}"/>
                                                <TextBlock Text="El nombre del equipo es obligatorio."
                                                           Foreground="{DynamicResource DangerBrush}"
                                                           FontSize="11"
                                                           Margin="2,6,0,0"
                                                           Visibility="{Binding IsNombreVacio, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            </StackPanel>
                                        </Grid>

                                        <Grid Margin="0,16,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="16"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Marca" Style="{StaticResource LabelTextStyle}"/>
                                                <ComboBox x:Name="CmbMarca"
                                                          IsEditable="True"
                                                          IsTextSearchEnabled="False"
                                                          ItemsSource="{Binding MarcasFiltradas}"
                                                          Text="{Binding FiltroMarca, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          Style="{StaticResource ModernComboBoxStyle}"
                                                          behaviors:ComboBoxEditableUpperBehavior.EnableUppercase="True"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="Estado" Style="{StaticResource LabelTextStyle}"/>
                                                <ComboBox ItemsSource="{Binding EstadosEquipo}"
                                                          SelectedItem="{Binding Estado, UpdateSourceTrigger=PropertyChanged}"
                                                          Style="{StaticResource ModernComboBoxStyle}"/>
                                            </StackPanel>
                                        </Grid>

                                        <StackPanel Margin="0,16,0,0">
                                            <TextBlock Text="Sede *" Style="{StaticResource LabelTextStyle}"/>
                                            <ComboBox ItemsSource="{Binding Sedes}"
                                                      SelectedItem="{Binding Sede, UpdateSourceTrigger=PropertyChanged}"
                                                      Style="{StaticResource ModernComboBoxStyle}"/>
                                            <TextBlock Text="La sede del equipo es obligatoria."
                                                       Foreground="{DynamicResource DangerBrush}"
                                                       FontSize="11"
                                                       Margin="2,6,0,0"
                                                       Visibility="{Binding IsSedeVacio, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardSectionStyle}" Margin="0,16,0,0">
                                    <StackPanel>
                                        <TextBlock Text="Compra" Style="{StaticResource SectionHeaderStyle}"/>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="16"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Fecha de compra" Style="{StaticResource LabelTextStyle}"/>
                                                <DatePicker x:Name="FechaCompraPicker"
                                                            SelectedDate="{Binding FechaCompra, UpdateSourceTrigger=PropertyChanged}"
                                                            SelectedDateChanged="OnFechaCompra_SelectedDateChanged"
                                                            Style="{StaticResource ModernDatePickerStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="Precio (COP)" Style="{StaticResource LabelTextStyle}"/>
                                                <TextBox Text="{Binding Precio, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource DecimalToCopCurrencyConverter}}"
                                                         Style="{StaticResource ModalTextBoxStyle}"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <Border Style="{StaticResource CardSectionStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Clasificación y proveedor" Style="{StaticResource SectionHeaderStyle}"/>

                                        <StackPanel>
                                            <TextBlock Text="Clasificación" Style="{StaticResource LabelTextStyle}"/>
                                            <ComboBox x:Name="CmbClasificacion"
                                                      IsEditable="True"
                                                      IsTextSearchEnabled="False"
                                                      ItemsSource="{Binding ClasificacionesFiltradas}"
                                                      Text="{Binding FiltroClasificacion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Style="{StaticResource ModernComboBoxStyle}"
                                                      behaviors:ComboBoxEditableUpperBehavior.EnableUppercase="True"/>
                                        </StackPanel>

                                        <StackPanel Margin="0,16,0,0">
                                            <TextBlock Text="Comprado a" Style="{StaticResource LabelTextStyle}"/>
                                            <ComboBox x:Name="CmbCompradoA"
                                                      IsEditable="True"
                                                      IsTextSearchEnabled="False"
                                                      ItemsSource="{Binding CompradoAFiltrados}"
                                                      Text="{Binding FiltroCompradoA, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Style="{StaticResource ModernComboBoxStyle}"
                                                      behaviors:ComboBoxEditableUpperBehavior.EnableUppercase="True"/>
                                        </StackPanel>

                                        <StackPanel Margin="0,16,0,0">
                                            <TextBlock Text="Frecuencia de mantenimiento" Style="{StaticResource LabelTextStyle}"/>
                                            <ComboBox ItemsSource="{Binding FrecuenciasMantenimiento}"
                                                      SelectedItem="{Binding FrecuenciaMtto, UpdateSourceTrigger=PropertyChanged}"
                                                      Style="{StaticResource ModernComboBoxStyle}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardSectionStyle}" Margin="0,16,0,0">
                                    <StackPanel>
                                        <TextBlock Text="Observaciones" Style="{StaticResource SectionHeaderStyle}"/>
                                        <TextBox x:Name="TxtObservaciones"
                                                 Text="{Binding Observaciones, UpdateSourceTrigger=PropertyChanged}"
                                                 TextWrapping="Wrap"
                                                 AcceptsReturn="True"
                                                 VerticalContentAlignment="Top"
                                                 VerticalScrollBarVisibility="Auto"
                                                 Style="{StaticResource ModalTextBoxStyle}"
                                                 Height="164"
                                                 MaxLength="1000"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2"
                        Padding="26,16"
                        Background="Transparent"
                        BorderBrush="{DynamicResource HeaderBottomBorderBrush}"
                        BorderThickness="0,1,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="Cancelar"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="OnCancelar_Click"
                                Margin="0,0,10,0"
                                MinWidth="120"
                                MinHeight="40"/>

                        <Button x:Name="BtnGuardar"
                                Content="Guardar"
                                Command="{Binding GuardarCommand}"
                                Click="OnGuardar_Click"
                                IsDefault="True"
                                IsEnabled="{Binding IsFormularioValido}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                MinWidth="140"
                                MinHeight="40"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>