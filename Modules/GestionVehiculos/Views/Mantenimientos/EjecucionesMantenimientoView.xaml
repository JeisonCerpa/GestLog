<UserControl x:Class="GestLog.Modules.GestionVehiculos.Views.Mantenimientos.EjecucionesMantenimientoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:GestLog.Converters"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter" />

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#118938" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#118938" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.12" BlurRadius="6" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#37AB4E" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#37AB4E" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#118938" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#118938" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#EDEDED" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" />
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Background="#FAFAFA">
        <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="16,16,16,8">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Ejecuciones de Mantenimiento" 
                           FontSize="18" FontWeight="Bold" Foreground="#504F4E" />
                <TextBlock Text="Historial y registro de ejecuciones de mantenimiento" 
                           FontSize="13" Foreground="#9D9D9C" Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16,8,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Filtros y Acciones -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <StackPanel Orientation="Vertical">
                    <!-- Filtro por Placa y Fechas -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Placa:" VerticalAlignment="Center" 
                                       Foreground="#504F4E" FontSize="12" FontWeight="SemiBold" Margin="0,0,6,0" />                            <TextBox Text="{Binding FilterPlaca, UpdateSourceTrigger=PropertyChanged}"
                                     Width="180" Height="32" Padding="8" FontSize="12"
                                     Background="White" BorderBrush="#CCCCCC" BorderThickness="1"
                                     Margin="0,0,20,0" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Desde:" VerticalAlignment="Center" 
                                       Foreground="#504F4E" FontSize="12" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <DatePicker SelectedDate="{Binding FilterFechaDesde}"
                                       Width="140" Height="32" Padding="4" FontSize="12"
                                       Background="White" BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,20,0" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Hasta:" VerticalAlignment="Center" 
                                       Foreground="#504F4E" FontSize="12" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <DatePicker SelectedDate="{Binding FilterFechaHasta}"
                                       Width="140" Height="32" Padding="4" FontSize="12"
                                       Background="White" BorderBrush="#CCCCCC" BorderThickness="1" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Botones de Acción -->
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="Cargar Todas" 
                                Command="{Binding LoadEjecucionesCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,8,0"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource BooleanToOpacityConverter}, ConverterParameter=Invert}" />
                        <Button Content="Historial por Placa" 
                                Command="{Binding LoadHistorialVehiculoCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,8,0"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource BooleanToOpacityConverter}, ConverterParameter=Invert}" />
                        <Button Content="Última Ejecución" 
                                Command="{Binding GetUltimaEjecucionCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,8,0"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource BooleanToOpacityConverter}, ConverterParameter=Invert}" />
                        <Button Content="Filtrar por Fechas" 
                                Command="{Binding FilterByFechasCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,8,0"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource BooleanToOpacityConverter}, ConverterParameter=Invert}" />                        <Button Content="Limpiar" 
                                Command="{Binding ClearSelectionCommand}"
                                Background="#9D9D9C"
                                Foreground="White"
                                Padding="12,8"
                                FontSize="13"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Cursor="Hand" />
                    </StackPanel>

                    <Border Background="#F7F9FB" BorderBrush="#DDE5EC" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,12,0,0">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Registro de mantenimiento" FontSize="12" FontWeight="Bold" Foreground="#504F4E" />
                            <TextBlock Text="El registro preventivo se gestiona en una ventana modal" FontSize="11" Foreground="#9D9D9C" Margin="0,6,0,0" />

                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <Button Content="Abrir registro preventivo"
                                        Click="BtnOpenPreventivoModal_Click"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Margin="0,0,8,0" />
                                <Button Content="Actualizar solo kilometraje"
                                        Command="{Binding ActualizarKilometrajeVehiculoCommand}"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        IsEnabled="{Binding IsLoading, Converter={StaticResource BooleanToOpacityConverter}, ConverterParameter=Invert}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Messages -->
                    <TextBlock Text="Cargando..." 
                               Foreground="#118938" FontSize="12" Margin="0,12,0,0"
                               Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <Border Background="#FFEBEE" CornerRadius="6" Padding="12" Margin="0,12,0,0"
                            Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="{Binding ErrorMessage}" 
                                   Foreground="#C0392B" FontSize="12" 
                                   TextWrapping="Wrap" />
                    </Border>

                    <Border Background="#E8F5E9" CornerRadius="6" Padding="12" Margin="0,12,0,0"
                            Visibility="{Binding SuccessMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="{Binding SuccessMessage}" 
                                   Foreground="#10B981" FontSize="12" 
                                   TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </Border>

            <!-- Lista de Ejecuciones -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <ListBox ItemsSource="{Binding Ejecuciones}" 
                         SelectedItem="{Binding SelectedEjecucion}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="6" Padding="12" 
                                    BorderBrush="#EDEDED" BorderThickness="1" Margin="0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="4" Orientation="Vertical">
                                        <TextBlock Text="{Binding PlacaVehiculo}" 
                                                   FontWeight="Bold" FontSize="13" Foreground="#118938" />
                                        <TextBlock Text="Fecha:" FontSize="11" Foreground="#9D9D9C" Margin="0,8,0,0" />
                                        <TextBlock Text="{Binding FechaEjecucion, StringFormat='F'}" 
                                                   FontSize="11" Foreground="#1D1D1B" />
                                        <TextBlock Text="KM al Momento:" FontSize="11" Foreground="#9D9D9C" Margin="0,8,0,0" />
                                        <TextBlock Text="{Binding KMAlMomento}" 
                                                   FontSize="11" Foreground="#1D1D1B" />
                                        <TextBlock Text="Responsable:" FontSize="11" Foreground="#9D9D9C" Margin="0,8,0,0" />
                                        <TextBlock Text="{Binding ResponsableEjecucion}" 
                                                   FontSize="11" Foreground="#1D1D1B" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Orientation="Vertical"
                                               VerticalAlignment="Top" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding Estado}" 
                                                   FontSize="11" FontWeight="SemiBold"
                                                   Foreground="#118938" />
                                        <TextBlock Text="{Binding Costo, StringFormat='${0:N2}'}" 
                                                   FontSize="11" Foreground="#1D1D1B" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
