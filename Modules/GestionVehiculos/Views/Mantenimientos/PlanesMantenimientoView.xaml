<UserControl x:Class="GestLog.Modules.GestionVehiculos.Views.Mantenimientos.PlanesMantenimientoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:GestLog.Converters"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter" />

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#118938" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#118938" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.12" BlurRadius="6" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#37AB4E" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#37AB4E" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#118938" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#118938" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#EDEDED" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PlanStatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#118938" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding EstadoPlan}" Value="Vencido">
                    <Setter Property="Foreground" Value="#C0392B" />
                </DataTrigger>
                <DataTrigger Binding="{Binding EstadoPlan}" Value="Próximo">
                    <Setter Property="Foreground" Value="#F59E0B" />
                </DataTrigger>
                <DataTrigger Binding="{Binding EstadoPlan}" Value="Sin datos">
                    <Setter Property="Foreground" Value="#9D9D9C" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Background="#FAFAFA">
        <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="16,16,16,8">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Planes de Mantenimiento" 
                           FontSize="18" FontWeight="Bold" Foreground="#504F4E" />
                <TextBlock Text="Configura el plan del vehículo actual" 
                           FontSize="13" Foreground="#9D9D9C" Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16,8,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Acciones -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Asignación de plan" FontSize="13" FontWeight="SemiBold" Foreground="#504F4E" />
                    <TextBlock Text="Gestiona el plan del vehículo en una ventana modal" FontSize="11" Foreground="#9D9D9C" Margin="0,6,0,0" />

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Abrir configuración de plan"
                                Click="BtnOpenPlanModal_Click"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,8,0" />
                        <Button Content="Recargar plan actual" 
                                Command="{Binding FilterByPlacaCommand}"
                                Style="{StaticResource PrimaryButtonStyle}" />
                    </StackPanel>

                    <Border Background="#F8F8F8" CornerRadius="6" Padding="10" Margin="0,12,0,0" BorderBrush="#E5E5E5" BorderThickness="1">
                        <TextBlock Text="{Binding PlanActualResumen}" Foreground="#504F4E" FontSize="11" TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </Border>

            <!-- Lista de Planes -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <ListBox ItemsSource="{Binding Planes}" 
                         SelectedItem="{Binding SelectedPlan}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="6" Padding="12" 
                                    BorderBrush="#EDEDED" BorderThickness="1" Margin="0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Orientation="Vertical">
                                        <TextBlock Text="{Binding PlacaVehiculo}" 
                                                   FontWeight="Bold" FontSize="13" Foreground="#118938" />
                                        <TextBlock Text="Próxima Ejecución:" FontSize="11" Foreground="#9D9D9C" Margin="0,8,0,0" />
                                        <TextBlock Text="{Binding ProximaFechaEjecucion, StringFormat='F'}" 
                                                   FontSize="11" Foreground="#1D1D1B" />
                                        <TextBlock Text="Próximo KM:" FontSize="11" Foreground="#9D9D9C" Margin="0,8,0,0" />
                                        <TextBlock Text="{Binding ProximoKMEjecucion}" 
                                                   FontSize="11" Foreground="#1D1D1B" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding EstadoPlan}" Style="{StaticResource PlanStatusTextStyle}" HorizontalAlignment="Right" />
                                        <TextBlock Text="{Binding EstadoPlanDetalle}" FontSize="10" Foreground="#9D9D9C" HorizontalAlignment="Right" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
