<Window x:Class="GestLog.Modules.GestionVehiculos.Views.Mantenimientos.PlanMantenimientoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:GestLog.Converters"
        Title="Configurar plan de mantenimiento"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="Manual"
        Width="1100"
        Height="720"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        Style="{StaticResource ModalWindowBase}">

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </Window.Resources>

    <Grid x:Name="RootGrid" Background="{DynamicResource ModalOverlayBrush}" MouseLeftButtonDown="Overlay_MouseLeftButtonDown">
        <Border Background="{DynamicResource BackgroundElevatedBrush}" CornerRadius="12" Padding="0"
                Width="1100" Height="720"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                BorderThickness="0" Effect="{StaticResource WindowShadow}" MouseLeftButtonDown="Panel_MouseLeftButtonDown"
                Style="{StaticResource ModalCardStyle}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" CornerRadius="12,12,0,0" Padding="24,20" Effect="{StaticResource HeaderShadow}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="{DynamicResource BackgroundSurfaceBrush}" BorderThickness="0" CornerRadius="8" Padding="8" Margin="0,0,16,0" Width="44" Height="44" Effect="{StaticResource SectionShadow}">
                                <TextBlock Text="🛠" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Style="{StaticResource ModalTextBlockBase}"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Foreground="White">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource HeaderTextStyle}">
                                            <Setter Property="Text" Value="Configurar plan de mantenimiento"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEditingSinglePlan}" Value="True">
                                                    <Setter Property="Text" Value="Editar plan de mantenimiento"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Style="{StaticResource SubHeaderTextStyle}" Foreground="White" Opacity="0.95" Margin="0,3,0,0"
                                           Text="{Binding KilometrajeActualVehiculo, StringFormat=Kilometraje actual del vehículo: {0:N0} km}"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="1" x:Name="BtnCloseHeader" Style="{StaticResource CloseButton}" Click="BtnCancel_Click" ToolTip="Cerrar (Esc)" HorizontalAlignment="Right" VerticalAlignment="Center" FocusVisualStyle="{x:Null}">
                            <Grid Width="14" Height="14">
                                <Line X1="0" Y1="0" X2="14" Y2="14" Stroke="White" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                <Line X1="14" Y1="0" X2="0" Y2="14" Stroke="White" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                            </Grid>
                        </Button>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" FocusVisualStyle="{x:Null}">
                    <Grid Margin="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="340" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- plantillas card list -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Plantillas" Style="{StaticResource SectionHeaderStyle}" Foreground="{DynamicResource TextBodyBrush}" />
                            <TextBlock Text="Selecciona una plantilla para configurar o editar un plan." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,8" TextWrapping="Wrap" />

                            <ItemsControl ItemsSource="{Binding PlantillasSeleccionables}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="140" Height="160" Padding="8" Margin="6" Background="{DynamicResource BackgroundSurfaceBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="6"
                                                Visibility="{Binding IsVisibleWhenEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Grid>
                                                <StackPanel>
                                                    <Grid Margin="0,0,0,2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="24" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding NombrePlantilla}" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,6,0" Foreground="{DynamicResource TextBodyBrush}" />
                                                        <Border Grid.Column="1" Width="20" Height="20" CornerRadius="10" Background="{DynamicResource PrimaryBrush}" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1"
                                                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                                                Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <TextBlock Text="✓" Foreground="White" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                    </Grid>
                                                    <TextBlock Text="{Binding IntervaloKMAplicado, StringFormat={}{0:N0} km}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" />
                                                    <TextBlock Text="{Binding IntervaloDiasAplicado, StringFormat={}{0} días}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" />
                                                    <TextBlock Text="{Binding NextKmPreview, StringFormat=Próx. km: {0:N0}}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" />
                                                    <Button Content="Configurar" Style="{StaticResource ThemeSecondaryButtonStyle}" Margin="0,8,0,0" Padding="8,4" FontSize="12"
                                                            Command="{Binding DataContext.SelectTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}" FocusVisualStyle="{x:Null}" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <!-- details and assigned plans -->
                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                            <Border Background="{DynamicResource BackgroundSurfaceBrush}" Padding="12" CornerRadius="6" Visibility="{Binding IsDetailVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding SelectedTemplate.NombrePlantilla}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" />
                                        <Button Grid.Column="1" Content="✕" Style="{StaticResource ThemeSecondaryButtonStyle}" Width="28" Height="28" Margin="8,0,0,0" Padding="3"
                                                Command="{Binding DeselectTemplateCommand}"
                                                CommandParameter="{Binding SelectedTemplate}"
                                                ToolTip="Deseleccionar plantilla" FocusVisualStyle="{x:Null}" />
                                    </Grid>
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextBodyBrush}" Margin="0,4,0,12">
                                        <Run Text="{Binding SelectedTemplate.IntervaloKMAplicado, StringFormat={}{0:N0} km}" />
                                        <Run Text="  /  " />
                                        <Run Text="{Binding SelectedTemplate.IntervaloDiasAplicado, StringFormat={}{0} días}" />
                                    </TextBlock>

                                    <!-- customization fields -->
                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="140" />
                                            <ColumnDefinition Width="140" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Cada cuántos KM" FontSize="10" Foreground="{DynamicResource TextBodyBrush}" HorizontalAlignment="Center" />
                                        <TextBlock Grid.Column="1" Text="Cada cuántos días" FontSize="10" Foreground="{DynamicResource TextBodyBrush}" HorizontalAlignment="Center" />
                                        <TextBlock Grid.Column="2" Text="KM del último cambio" FontSize="10" Foreground="{DynamicResource TextBodyBrush}" HorizontalAlignment="Center" />
                                        <TextBlock Grid.Column="3" Text="Fecha del último cambio" FontSize="10" Foreground="{DynamicResource TextBodyBrush}" HorizontalAlignment="Center" />
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="140" />
                                            <ColumnDefinition Width="140" />
                                        </Grid.ColumnDefinitions>

                                        <TextBox Grid.Column="0" Text="{Binding SelectedTemplate.IntervaloKMPersonalizadoInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}" Margin="0,0,6,0" VerticalContentAlignment="Center" ToolTip="KM personalizado (opcional)" />
                                        <TextBox Grid.Column="1" Text="{Binding SelectedTemplate.IntervaloDiasPersonalizadoInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}" Margin="0,0,6,0" VerticalContentAlignment="Center" ToolTip="Días personalizados (opcional)" />
                                        <TextBox Grid.Column="2" Text="{Binding SelectedTemplate.UltimoKMRegistradoInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}" Margin="0,0,6,0" VerticalContentAlignment="Center" ToolTip="Último KM de esta plantilla" />
                                        <DatePicker Grid.Column="3" SelectedDate="{Binding SelectedTemplate.UltimaFechaMantenimiento, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeDatePickerStyle}" ToolTip="Última fecha de mantenimiento de esta plantilla" />
                                    </Grid>
                                    <TextBlock Text="{Binding SelectedTemplate.NextKmFormula}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0" />
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardSectionStyle}" Margin="0,12,0,0">
                                <StackPanel>
                                    <TextBlock Text="Planes asignados" Style="{StaticResource SectionHeaderStyle}" Foreground="{DynamicResource TextBodyBrush}" />
                                    <!-- cards instead of listbox -->
                                    <ItemsControl ItemsSource="{Binding Planes}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="220" Padding="10" Margin="4" CornerRadius="6" BorderThickness="1"
                                                        Background="{DynamicResource BackgroundSurfaceBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}">
                                                    <StackPanel>
                                                    <TextBlock FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Text="{Binding PlantillaNombre}" TextWrapping="Wrap" />
                                                    <TextBlock FontSize="11" Foreground="{DynamicResource TextBodyBrush}" Text="{Binding EstadoPlan}" />
                                                    <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding ProximoKMEjecucion, StringFormat=Próx. KM: {0:N0}}" />
                                                    <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding ProximaFechaEjecucion, StringFormat=Próx. Fecha: {0:dd/MM/yyyy}}" />
                                                        <Grid Margin="0,8,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <Button Grid.Column="0" Content="Editar" Style="{StaticResource ThemeSecondaryButtonStyle}"
                                                                    Command="{Binding DataContext.EditPlanCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}" Margin="0,0,4,0" FocusVisualStyle="{x:Null}" />
                                                            <Button Grid.Column="1" Content="Eliminar" Style="{StaticResource ThemeDangerButtonStyle}"
                                                                    Command="{Binding DataContext.DeletePlanCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}" Margin="4,0,0,0" FocusVisualStyle="{x:Null}" />
                                                        </Grid>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <TextBlock Text="Estado actual" Style="{StaticResource SectionHeaderStyle}" Margin="0,4,0,4" Foreground="{DynamicResource TextBodyBrush}" />
                                    <Border Background="{DynamicResource BackgroundSurfaceBrush}" CornerRadius="6" Padding="10" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1">
                                        <TextBlock Text="{Binding PlanActualResumen}" Foreground="{DynamicResource TextBodyBrush}" FontSize="11" TextWrapping="Wrap" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource DangerBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,12"
                                    Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                                <TextBlock Text="{Binding ErrorMessage}" Foreground="White" FontWeight="SemiBold" FontSize="12" TextWrapping="Wrap" />
                            </Border>

                            <Border Background="{DynamicResource PrimaryBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,12"
                                    Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibilityConverter}}">
                                <TextBlock Text="{Binding SuccessMessage}" Foreground="White" FontWeight="SemiBold" FontSize="12" TextWrapping="Wrap" />
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>

                <Border Grid.Row="2" Background="{DynamicResource BackgroundSurfaceBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1,1,1,0" Padding="16" CornerRadius="0,0,12,12">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancelar" Style="{StaticResource ThemeSecondaryButtonStyle}" Margin="0,0,8,0" Click="BtnCancel_Click" FocusVisualStyle="{x:Null}" />
                        <!-- sólo mostrar botón de eliminar cuando estamos editando un único plan -->
                        <Button Content="🗑 Eliminar" Style="{StaticResource ThemeDangerButtonStyle}" Margin="0,0,8,0"
                                Command="{Binding DeletePlanCommand}"
                                CommandParameter="{Binding SelectedPlan}"
                                Visibility="{Binding IsEditingSinglePlan, Converter={StaticResource BooleanToVisibilityConverter}}" FocusVisualStyle="{x:Null}" />
                        <Button Content="Guardar plan" Style="{StaticResource ThemePrimaryButtonStyle}" Command="{Binding GuardarPlanVehiculoCommand}"
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" FocusVisualStyle="{x:Null}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
