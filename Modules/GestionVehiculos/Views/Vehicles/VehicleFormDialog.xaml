<Window x:Class="GestLog.Modules.GestionVehiculos.Views.Vehicles.VehicleFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:GestLog.Converters"
        mc:Ignorable="d"
        Title="{Binding TituloDialog}" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="Manual"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        Style="{StaticResource ModalWindowBase}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed"
        RenderOptions.BitmapScalingMode="HighQuality"
        RenderOptions.ClearTypeHint="Enabled">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/ModalWindowsStandard.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <conv:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
            <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <conv:InverseBooleanConverter x:Key="InverseBooleanConverter" />
            <conv:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        </ResourceDictionary>    </Window.Resources>

    <Grid x:Name="RootGrid" Background="#CC000000" MouseLeftButtonDown="Overlay_MouseLeftButtonDown" UseLayoutRounding="True" SnapsToDevicePixels="True">
        <!-- Panel centrado modal - Adaptable al contenido -->
        <Border x:Name="Card" Background="{DynamicResource BackgroundSurfaceBrush}" CornerRadius="12" Padding="0" MaxWidth="1100" MaxHeight="900"
                HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource WindowShadow}" MouseLeftButtonDown="Panel_MouseLeftButtonDown"
                UseLayoutRounding="True" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="HighQuality">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header Modal -->
                <Border Grid.Row="0" Background="{DynamicResource BackgroundElevatedBrush}" CornerRadius="12,12,0,0" Padding="28,24" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" UseLayoutRounding="True" SnapsToDevicePixels="True">
                    <Grid UseLayoutRounding="True" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="🚗" FontSize="28" Margin="0,0,16,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding TituloDialog}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBodyBrush}"/>
                                <TextBlock Text="Gestiona información detallada del vehículo" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="1" x:Name="BtnCerrarHeader" Click="BtnCerrar_Click" ToolTip="Cerrar (Esc)" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="8" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <TextBlock Text="✕" FontSize="20" Foreground="{DynamicResource TextBodyBrush}"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- Contenido - Layout Split: Foto Izquierda + Campos Derecha -->
                <Grid Grid.Row="1" UseLayoutRounding="True" SnapsToDevicePixels="True" Margin="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="350" />
                        <ColumnDefinition Width="420" />
                    </Grid.ColumnDefinitions>

                    <!-- Columna Izquierda: Foto y Controles -->
                    <StackPanel Grid.Column="0" Margin="0,0,24,0">
                        <TextBlock Text="📸 FOTO DEL VEHÍCULO" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,12"/>
                        
                        <!-- Preview de Foto -->
                        <Border Height="280" Background="{DynamicResource BackgroundSurfaceBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="8" ClipToBounds="True" Margin="0,0,0,12">
                            <Image Source="{Binding PhotoThumbPath}" Stretch="Uniform" />
                        </Border>

                        <!-- Botón Seleccionar -->
                        <Button Content="📷 Seleccionar foto" Command="{Binding SelectPhotoCommand}" Style="{StaticResource ThemePrimaryButtonStyle}" Padding="16,10" FontSize="12" Margin="0,0,0,8"/>
                        
                        <!-- Info -->
                        <TextBlock Text="Formatos: JPG, PNG" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" TextWrapping="Wrap"/>
                        <TextBlock Text="Máximo 5MB (se comprime automáticamente)" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Columna Derecha: Campos de Información -->
                    <StackPanel Grid.Column="1">
                        <!-- Información Básica -->
                        <TextBlock Text="📋 INFORMACIÓN BÁSICA" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,12"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Placa y VIN -->
                            <StackPanel Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="Placa *" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtPlaca" Text="{Binding Plate, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="2">
                                <TextBlock Text="VIN *" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtVin" Text="{Binding Vin, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}"/>
                            </StackPanel>
                            
                            <!-- Marca y Modelo -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,8,0,0">
                                <TextBlock Text="Marca *" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <ComboBox x:Name="CmbMarca" ItemsSource="{Binding MarcasFiltradas, UpdateSourceTrigger=PropertyChanged}" Text="{Binding Brand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEditable="True" IsTextSearchEnabled="True" TextSearch.TextPath="." Style="{StaticResource ThemeComboBoxStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,8,0,0">
                                <TextBlock Text="Modelo *" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtModelo" Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}"/>
                            </StackPanel>
                            
                            <!-- Versión y Año -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,8,0,0">
                                <TextBlock Text="Versión" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtVersion" Text="{Binding Version, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,8,0,0">
                                <TextBlock Text="Año" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtAno" Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}"/>
                            </StackPanel>
                            
                            <!-- Color y Kilometraje -->
                            <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,8,0,0">
                                <TextBlock Text="Color" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtColor" Text="{Binding Color, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemeTextBoxStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,8,0,0">
                                <TextBlock Text="Kilometraje (KM)" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtKilometraje" Text="{Binding Mileage, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Style="{StaticResource ThemeTextBoxStyle}"/>
                            </StackPanel>
                        </Grid>

                        <!-- Clasificación -->
                        <TextBlock Text="⚙️  CLASIFICACIÓN" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,12,0,12"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Tipo y Estado -->
                            <StackPanel Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="Tipo de Vehículo *" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <ComboBox x:Name="CmbTipo" ItemsSource="{Binding VehicleTypes}" SelectedItem="{Binding SelectedType}" Style="{StaticResource ThemeComboBoxStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="2">
                                <TextBlock Text="Estado *" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <ComboBox x:Name="CmbEstado" ItemsSource="{Binding VehicleStates}" SelectedItem="{Binding SelectedState}" Style="{StaticResource ThemeComboBoxStyle}"/>
                            </StackPanel>
                            
                            <!-- Combustible -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,8,0,0">
                                <TextBlock Text="Tipo de Combustible" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBodyBrush}" Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding FuelTypes}" SelectedItem="{Binding FuelType}" Style="{StaticResource ThemeComboBoxStyle}" IsEditable="False"/>
                            </StackPanel>
                        </Grid>

                        <!-- Mensajes -->
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#C0392B" Style="{StaticResource SectionHeaderStyle}" TextWrapping="Wrap" Margin="0,12,0,0" Visibility="{Binding ErrorMessage, Converter={StaticResource CountToVisibilityConverter}}"/>
                        <TextBlock Text="{Binding SuccessMessage}" Foreground="#10B981" Style="{StaticResource SectionHeaderStyle}" TextWrapping="Wrap" Margin="0,12,0,0" Visibility="{Binding SuccessMessage, Converter={StaticResource CountToVisibilityConverter}}"/>
                    </StackPanel>
                </Grid>

                <!-- Footer con botones -->
                <Border Grid.Row="2" Background="{DynamicResource BackgroundElevatedBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,1,0,0" Padding="24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Botón Cancelar -->
                        <Button Grid.Column="1" 
                                Content="Cancelar" 
                                Click="BtnCancelar_Click"
                                Style="{StaticResource ThemeSecondaryButtonStyle}"
                                Padding="20,10" 
                                FontSize="13" 
                                Margin="0,0,10,0"/>

                        <!-- Botón Guardar -->
                        <Button Grid.Column="2" 
                                Content="💾 Guardar" 
                                Command="{Binding SaveCommand}"
                                Style="{StaticResource ThemePrimaryButtonStyle}"
                                Padding="20,10" 
                                FontSize="13"
                                IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
